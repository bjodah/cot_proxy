variables:
  - &ci_image 'cont-reg.bjodah.se:443/bjodah/triceratops-4:25'

when:
  - event: [push]

steps:
  - name: run-tests
    image: *ci_image
    commands:
      - bash -lc "source /opt-3/cpython-v3.*-apt-deb/bin/activate; pip install --upgrade-strategy=only-if-needed pytest-flask responses && pytest"
